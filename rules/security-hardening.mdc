---
description: 安全底线检查 — 每次代码变更自动触发
globs: "**/*"
alwaysApply: true
---

# Security Hardening

每次代码变更必须检查以下 6 条：

1. **用户输入** → 是否经过 sanitize/校验（禁止裸传到后端或数据库）
2. **SQL 查询** → 是否参数化（禁止字符串拼接 SQL）
3. **密钥/Token** → 是否出现在代码、日志、Git 提交中
4. **错误消息** → 是否泄露内部信息（路径、栈、版本号、数据库结构）
5. **文件上传** → 是否限制类型、大小、路径（防目录穿越）
6. **外部URL** → 是否校验白名单（防 SSRF）

违反任何一条的代码不得合并。
